#!/usr/bin/env sh
set -e

echo "Starting checkout DB migrations (demo stub)"
# In a real setup, invoke Alembic here; for SQLite demo we can just print
echo "No migrations to apply for demo (SQLite auto-create)."
echo "Migrations completed."


